# docker-warm-up

```
docker run hello-world

docker create hello-world

docker start id

docker start -a id

docker ps

docker ps --all

docker -v

docker version
```

```
docker run busybox ls

docker system prune

docker logs id
```
